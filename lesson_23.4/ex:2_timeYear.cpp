#include <iostream>// для ввода и вывода

// ============================================================================
// НАСТРОЙКА СЕЗОНА (ПРОГРАММИСТ ВЫБИРАЕТ ОДИН ИЗ ВАРИАНТОВ)
// ============================================================================

// Раскомментируйте ТОЛЬКО ОДНУ из следующих строк, чтобы выбрать сезон:

#define SPRING// Весна
// #define SUMMER// Лето
// #define AUTUMN  // Осень
// #define WINTER  // Зима

// ============================================================================
// ПРОВЕРКА КОРРЕКТНОСТИ НАСТРОЕК (ЗАЩИТА ОТ ОШИБОК)
// ============================================================================

// Директива #error - выдаёт ошибку компиляции с указанным сообщением
// Используем её, если программист не выбрал ни одного сезона ИЛИ выбрал несколько

#if defined(SPRING) && (defined(SUMMER) || defined(AUTUMN) || defined(WINTER))
#error "ОШИБКА: Выбрано несколько сезонов! Раскомментируйте только один макрос."

#elif defined(SUMMER) && (defined(SPRING) || defined(AUTUMN) || defined(WINTER))
#error "ОШИБКА: Выбрано несколько сезонов! Раскомментируйте только один макрос."

#elif defined(AUTUMN) && (defined(SPRING) || defined(SUMMER) || defined(WINTER))
#error "ОШИБКА: Выбрано несколько сезонов! Раскомментируйте только один макрос."

#elif defined(WINTER) && (defined(SPRING) || defined(SUMMER) || defined(AUTUMN))
#error "ОШИБКА: Выбрано несколько сезонов! Раскомментируйте только один макрос."

#elif !defined(SPRING) && !defined(SUMMER) && !defined(AUTUMN) && !defined(WINTER)
#error "ОШИБКА: Не выбран ни один сезон! Раскомментируйте один из макросов."

#endif

// ============================================================================
// ОСНОВНАЯ ФУНКЦИЯ (ОБЁРНУТА В УСЛОВНУЮ ДИРЕКТИВУ ПРЕПРОЦЕССОРА)
// ============================================================================

// Всю функцию main оборачиваем в условную компиляцию
// Препроцессор включит в компиляцию только тот код, который соответствует
// выбранному макросу. Остальные варианты даже не попадут в компилятор!

#if defined(SPRING)

// Этот код скомпилируется ТОЛЬКО если определён макрос SPRING
int main()
{
    std::cout << "Spring" << std::endl;// Весна
    return 0;
}

#elif defined(SUMMER)

// Этот код скомпилируется ТОЛЬКО если определён макрос SUMMER
int main()
{
    std::cout << "Summer" << std::endl;// Лето
    return 0;
}

#elif defined(AUTUMN)

// Этот код скомпилируется ТОЛЬКО если определён макрос AUTUMN
int main()
{
    std::cout << "Autumn" << std::endl;// Осень
    return 0;
}

#elif defined(WINTER)

// Этот код скомпилируется ТОЛЬКО если определён макрос WINTER
int main()
{
    std::cout << "Winter" << std::endl;// Зима
    return 0;
}

#endif
